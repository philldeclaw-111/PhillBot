# Component Library — PhillBot

Purpose: Define reusable bot modules, interfaces, and their usage rules (commands, message formats, pipelines).

Status: Drafted; must be populated with real components.
Open Items / Remaining Work:
- Add component definitions once bot codebase is present (Telegram handler, teaching pipeline, scheduler, etc.).

---

## Component Name (placeholder)

Status: Drafted; not implemented.
Open Items / Remaining Work:
- Add component purpose and usage rules.

**Purpose:**  
**Inputs/Props:**  
**Outputs/Events:**  
**States:**  
**Usage Rules:**  

---

## Future components (to add)

- **Telegram handler** — Receives messages, parses commands, dispatches to pipelines.
- **Teaching pipeline** — Subject + PDFs → teaching flow, podcast-style output.
- **Scheduler** — Regular texting/notifications to user.
- **GitHub automation** — Use of `gh` (and optionally Git) for code/repo operations; auth via `GH_TOKEN`.

Status: Drafted.
Open Items / Remaining Work:
- Populate when implementation begins.
